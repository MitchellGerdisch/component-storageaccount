{
  "name": "@custom/storage-component",
  "version": "1.0.0",
  "description": "A Pulumi component for Azure Storage Account with blob container and blob",
  "main": "bin/index.js",
  "types": "bin/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "pulumi",
    "azure",
    "storage",
    "component"
  ],
  "author": "",
  "license": "Apache-2.0",
  "dependencies": {
    "@pulumi/pulumi": "^3.0.0",
    "@pulumi/azure-native": "^2.0.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
